<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cl.aquilotienes.mapudungun">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NumerosActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">

        </activity>



        <activity
            android:name=".CategoriasActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".TodoCategoriasActivity"
            android:parentActivityName=".CategoriasActivity" />
        <activity
            android:name=".Abc_Activity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".PalabrasActivity"
            android:parentActivityName=".Abc_Activity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>